document.getElementById('manager-login')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,button=form.querySelector('button'),status=document.getElementById('login-status');button.disabled=true;button.textContent='Sending…';status.textContent='';try{const response=await fetch('/api/manager/request-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.getElementById('manager-email').value,website:document.getElementById('manager-website').value})}),result=await response.json();status.textContent=result.message||'Check your email.';if(!response.ok)throw new Error();form.querySelector('input[type=email]').disabled=true;}catch{if(!status.textContent)status.textContent='We couldn’t send the link. Please try again.';button.disabled=false;button.textContent='Send secure link';}});
document.addEventListener('click',async e=>{const button=e.target.closest('[data-feedback-id]');if(!button)return;button.disabled=true;const response=await fetch('/api/manager/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:button.dataset.feedbackId,status:button.dataset.feedbackStatus})});if(response.ok)location.reload();else{button.disabled=false;alert('Could not update this item.');}});
document.getElementById('manager-send-summary')?.addEventListener('click',async e=>{const button=e.currentTarget,status=document.getElementById('manager-status');button.disabled=true;button.textContent='Sending…';const response=await fetch('/api/manager/send-summary',{method:'POST'}),result=await response.json();status.textContent=result.message||'Done.';button.disabled=false;button.textContent='Email weekly report';});
