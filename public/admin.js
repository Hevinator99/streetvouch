document.addEventListener('click',async e=>{const button=e.target.closest('[data-status][data-id]');if(button){button.disabled=true;const response=await fetch('/api/admin/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:button.dataset.id,status:button.dataset.status})});if(response.ok)location.reload();else{button.disabled=false;alert('Could not update this item. Please try again.');}return;}const filter=e.target.closest('[data-filter]');if(filter){document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));filter.classList.add('active');document.querySelectorAll('.feedback-card').forEach(card=>{const choice=filter.dataset.filter;card.hidden=choice!=='all'&&card.dataset.status!==choice&&!(choice==='attention'&&card.dataset.severity!=='normal');});}});
document.getElementById('review-count')?.addEventListener('submit',async e=>{e.preventDefault();const button=e.currentTarget.querySelector('button');button.disabled=true;const response=await fetch('/api/admin/review-count',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({count:Number(document.getElementById('count').value)})});if(response.ok)location.reload();else{button.disabled=false;alert('Could not update the count.');}});
document.getElementById('send-summary')?.addEventListener('click',async e=>{const button=e.currentTarget,status=document.getElementById('admin-status');button.disabled=true;button.textContent='Sending…';status.textContent='';try{const response=await fetch('/api/admin/send-summary',{method:'POST'}),result=await response.json();status.textContent=result.message||'Done.';if(!response.ok)throw new Error();}catch{if(!status.textContent)status.textContent='The summary could not be sent.';}finally{button.disabled=false;button.textContent='Email weekly summary';}});
