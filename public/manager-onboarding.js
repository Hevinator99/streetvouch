const managerStatus=document.getElementById('manager-onboarding-status')||document.getElementById('login-status');
const managerMessage=message=>{if(managerStatus)managerStatus.textContent=message;};
document.getElementById('accept-invitation')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button'),password=document.getElementById('invite-password').value,confirmation=document.getElementById('invite-confirmation').value;button.disabled=true;const response=await fetch('/api/manager/accept-invite',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:document.getElementById('invitation-token').value,password,confirmation})}),result=await response.json();if(response.ok)location.href=result.redirect;else{managerMessage(result.message||'Invitation could not be accepted.');button.disabled=false;}});
document.getElementById('business-manager-login')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button');button.disabled=true;const response=await fetch('/api/manager/business-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:form.dataset.slug,email:document.getElementById('business-login-email').value,password:document.getElementById('business-login-password').value})}),result=await response.json();if(response.ok)location.href=result.redirect;else{managerMessage(result.message||'Sign-in failed.');button.disabled=false;}});
document.getElementById('manager-page-form')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,submitter=event.submitter,body=Object.fromEntries(new FormData(form));body.action=submitter?.name==='approve'?'approve_page':'save_page';form.querySelectorAll('button').forEach(button=>button.disabled=true);const response=await fetch('/api/manager/onboarding',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),result=await response.json();managerMessage(result.message||'Done.');if(response.ok)setTimeout(()=>location.reload(),500);else form.querySelectorAll('button').forEach(button=>button.disabled=false);});
