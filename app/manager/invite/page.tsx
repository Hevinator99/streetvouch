import Script from "next/script";
export const dynamic="force-dynamic";
export default async function InvitePage({searchParams}:{searchParams:Promise<{token?:string}>}){const token=(await searchParams).token??"";return <><link rel="stylesheet" href="/manager.css"/><link rel="stylesheet" href="/manager-extra.css"/><main className="manager-login"><div className="login-brand"><span>✓</span>streetvouch</div><div className="login-card"><span className="kicker">SECURE INVITATION</span><h1>Set up your manager account.</h1><p>Create a password to accept the invitation. StreetVouch will never email or display your password.</p><form id="accept-invitation"><input type="hidden" id="invitation-token" value={token}/><label htmlFor="invite-password">New password</label><input id="invite-password" type="password" minLength={10} required autoComplete="new-password"/><label htmlFor="invite-confirmation">Confirm password</label><input id="invite-confirmation" type="password" minLength={10} required autoComplete="new-password"/><button>Accept invitation</button><p id="login-status" role="status"></p></form></div></main><Script src="/manager-onboarding.js" strategy="afterInteractive"/></>}
