import { classifyFeedbackSeverity } from "../../../../lib/security";
import { allow, anonymousKey, db, json, sameOrigin } from "../../_shared";
export async function POST(request:Request){if(!sameOrigin(request))return json({ok:false},403);const body=await request.json() as {business?:string;message?:string;name?:string;email?:string;contactRequested?:boolean;asset?:string;test?:boolean};if(body.test)return json({ok:true,test:true,message:"Safe test complete. Nothing was stored or sent."});const message=(body.message??'').trim();if(!body.business||message.length<3||message.length>2000)return json({ok:false,message:"Enter between 3 and 2,000 characters."},400);const database=db(),business=await database.prepare("SELECT id FROM businesses WHERE slug=? AND status='active' AND page_approved=1").bind(body.business).first<{id:string}>();if(!business)return json({ok:false},404);if(!await allow(database,`customer-feedback:${await anonymousKey(request,'feedback')}`,5,3600))return json({ok:false,message:"Too many submissions. Try again later."},429);const email=(body.email??'').trim().toLowerCase();if(body.contactRequested&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return json({ok:false,message:"Enter an email address for the reply."},400);const now=new Date().toISOString(),id=crypto.randomUUID();await database.batch([database.prepare("INSERT INTO feedback (id,business_id,customer_name,customer_email,message,contact_requested,severity,status,created_at) VALUES (?,?,?,?,?,?,?,'new',?)").bind(id,business.id,(body.name??'').trim().slice(0,100)||null,body.contactRequested?email:null,message,body.contactRequested?1:0,classifyFeedbackSeverity(message),now),database.prepare("INSERT INTO events (id,business_id,event_type,asset_id,created_at) VALUES (?,?,'private_submission',?,?)").bind(crypto.randomUUID(),business.id,body.asset||null,now)]);return json({ok:true,message:"Thank you. Your private feedback has been sent securely to the business."})}
