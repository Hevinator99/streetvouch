import { env } from "cloudflare:workers";
import { getManagerSession, hashToken } from "../../../manager-auth";
import { db } from "../../_shared";
export async function GET(request:Request){const session=await getManagerSession();if(!session)return Response.redirect(new URL("/manager/village-barbers-cobham",request.url),303);if(!env.GOOGLE_CLIENT_ID||!env.GOOGLE_CLIENT_SECRET||!env.GOOGLE_TOKEN_ENCRYPTION_KEY){const url=new URL("/manager/village-barbers-cobham",request.url);url.searchParams.set("google","setup_required");return Response.redirect(url,303);}const state=crypto.randomUUID()+crypto.randomUUID().replaceAll("-","");await db().prepare("INSERT INTO google_oauth_states (state_hash,business_id,manager_user_id,expires_at,created_at) VALUES (?,?,?,?,?)").bind(await hashToken(state),session.businessId,session.managerUserId,new Date(Date.now()+10*60*1000).toISOString(),new Date().toISOString()).run();const redirectUri=`${new URL(request.url).origin}/api/google/callback`;const auth=new URL("https://accounts.google.com/o/oauth2/v2/auth");auth.search=new URLSearchParams({client_id:env.GOOGLE_CLIENT_ID,redirect_uri:redirectUri,response_type:"code",scope:"https://www.googleapis.com/auth/business.manage",access_type:"offline",prompt:"consent",state}).toString();return Response.redirect(auth,303);}
