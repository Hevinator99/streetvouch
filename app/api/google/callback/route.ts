import { env } from "cloudflare:workers";
import { db } from "../../_shared";
import { getManagerSession, hashToken } from "../../../manager-auth";
import { encryptToken, findFirstLocation, syncGoogleReviews } from "../../../google";
export async function GET(request:Request){const manager=await getManagerSession(),url=new URL(request.url),code=url.searchParams.get("code"),state=url.searchParams.get("state");if(!manager||!code||!state)return fail(request,"invalid");const database=db(),now=new Date().toISOString(),stateHash=await hashToken(state),saved=await database.prepare("SELECT business_id,manager_user_id FROM google_oauth_states WHERE state_hash=? AND expires_at>?").bind(stateHash,now).first<{business_id:string;manager_user_id:string}>();if(!saved||saved.business_id!==manager.businessId||saved.manager_user_id!==manager.managerUserId)return fail(request,"invalid");await database.prepare("DELETE FROM google_oauth_states WHERE state_hash=?").bind(stateHash).run();try{const redirectUri=`${url.origin}/api/google/callback`,response=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({client_id:env.GOOGLE_CLIENT_ID??"",client_secret:env.GOOGLE_CLIENT_SECRET??"",code,grant_type:"authorization_code",redirect_uri:redirectUri})});if(!response.ok)throw new Error(`OAuth token exchange failed (${response.status})`);const tokens=await response.json() as {access_token?:string;refresh_token?:string};if(!tokens.access_token||!tokens.refresh_token)throw new Error("Google did not return offline access");const location=await findFirstLocation(tokens.access_token),encrypted=await encryptToken(tokens.refresh_token),timestamp=new Date().toISOString();await database.prepare(`INSERT INTO google_connections (id,business_id,google_account_name,google_location_name,google_location_title,encrypted_refresh_token,token_iv,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?,'connected',?,?) ON CONFLICT(business_id) DO UPDATE SET google_account_name=excluded.google_account_name,google_location_name=excluded.google_location_name,google_location_title=excluded.google_location_title,encrypted_refresh_token=excluded.encrypted_refresh_token,token_iv=excluded.token_iv,status='connected',last_error=NULL,updated_at=excluded.updated_at`).bind(crypto.randomUUID(),manager.businessId,location.accountName,location.locationName,location.title,encrypted.encrypted,encrypted.iv,timestamp,timestamp).run();await syncGoogleReviews(manager.businessId);return redirect(request,"connected");}catch(error){console.error("google_callback_failed",error);await database.prepare(`INSERT INTO google_connections (id,business_id,status,last_error,created_at,updated_at) VALUES (?,?,'needs_attention',?,?,?) ON CONFLICT(business_id) DO UPDATE SET status='needs_attention',last_error=excluded.last_error,updated_at=excluded.updated_at`).bind(crypto.randomUUID(),manager.businessId,"Google connection failed",now,now).run();return redirect(request,"failed");}}
const redirect=(request:Request,status:string)=>Response.redirect(new URL(`/manager/village-barbers-cobham?google=${status}`,request.url),303);
const fail=(request:Request,status:string)=>redirect(request,status);
